import pandas as pd
new_1 = []
new_list = []
for key, value in mydict1.items():
    new_1 = list(key)
    new_1.append(value)
    new_list.append(new_1)

num_list = []
for t in text:
    for i in new_list:
        if t==i[0]:            
            new_1 = i
            if code[t] not in new_1:
                new_1.append(code[t])
                num_list.append(new_1)
                #print(num_list)
print(num_list, '\n')


sum = 0
count = 0
for i in num_list:
    sum += i[1]*len(i[2])
    count += i[1]
    #print(i[1], i[2])
print(sum)
print(count)
